PORT=3001
PROFILE=mock
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
# For PROFILE=aws, set real AWS keys so Rekognition CompareFaces can run
S3_ENDPOINT=http://localhost:4566
S3_BUCKET=local-uploads
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=upload_app
